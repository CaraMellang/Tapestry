import React from "react";
import styled from "styled-components";

export default function SearchList() {
  return <SearchListWrap>search list</SearchListWrap>;
}

const SearchListWrap = styled.div``;
